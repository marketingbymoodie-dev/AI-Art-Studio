node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz
.env
.env.*

# GeoLite2 Country DB (MaxMind license — download at boot / weekly refresh)
data/*.mmdb
data/*.mmdb.tmp-*

# Local dev artifacts (calibration runs, render tests, generated panel sources, etc.)
tmp/
tmp_dev_log*.txt
.commit-msg.tmp

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

